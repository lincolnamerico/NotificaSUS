> **⚠️ Documento histórico — snapshot da sessão de deploy em 2026-07-29.**
> Para status atual do projeto, consulte `docs/prd.md`, `docs/architecture.md`
> e as stories em `docs/stories/`. O artifact YAML correspondente está em
> `.aiox/handoffs/deploy-2026-07-29.yaml`.

# Handoff Session — 2026-07-29

## Resumo

Configuracao completa do ambiente de deploy e contas para o NotificaSUS. O projeto ja estava implementado localmente com todas as 3 Epics completas (fundacao, dashboard, graficos/exportacao). Nesta sessao:

1. Contas criadas (Vercel, Neon, Google OAuth)
2. Varaveis de ambiente configuradas no Vercel
3. Schema do banco aplicado no Neon
4. Deploy realizado em https://notificasus.vercel.app
5. Documentacao atualizada (PRD, Architecture)

## Contas Criadas

| Servico | Email | URL |
|---------|-------|-----|
| Vercel | lincolnamerico@gmail.com | https://vercel.com/gegpo/notificasus |
| Neon PostgreSQL | lincolnamerico@gmail.com | https://console.neon.tech |
| Google Cloud Console | lincolnamerico@gmail.com | https://console.cloud.google.com/apis/credentials |
| GitHub | lincolnamerico@gmail.com | https://github.com/lincolnamerico/NotificaSUS |

## Status do Projeto

### Completos (Epics 1-3, toda a implementacao)
- **Epic 1** — Next.js + Neon + Vercel, rota /notificar, middleware LGPD, formulario 3 passos, confirmacao com protocolo
- **Epic 2** — OAuth Google, dashboard KPIs por USF, segregacao de subdominio
- **Epic 3** — Schema estendido, graficos Recharts (barras, pizza, linha), exportacao CSV/JSON com anonimizacao LGPD, RBAC (visualizador bloqueado), 81 testes passando

### Deploy
- **URL:** https://notificasus.vercel.app
- **Formulario:** https://notificasus.vercel.app/notificar
- **Login:** https://notificasus.vercel.app/gestao/login
- **Dashboard:** https://notificasus.vercel.app/gestao/dashboard

### Funciona agora
- Formulario publico de notificacao (3 passos)
- Confirmacao com protocolo
- API de notificacao + anonimizacao
- API de USFs
- Login Google OAuth (requer email @pinhais.pr.gov.br)
- Dashboard com KPIs e graficos
- Exportacao CSV/JSON (admin/gestor apenas)
- Middleware de protecao de rotas

### Bloqueado / Nao funciona
- **Login dashboard:** Nao ha usuarios com email @pinhais.pr.gov.br — impossivel testar OAuth sem email da prefeitura
- **Subdominio gestao:** DNS de pinhais.pr.gov.br nao configurado
- **QR Code:** Nao impresso (depende da Secretaria de Saude)

## Varaveis de Ambiente (Vercel Production)

```env
DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require
AUTH_SECRET=<gerado via npx auth secret>
AUTH_GOOGLE_ID=<Google OAuth Client ID>
AUTH_GOOGLE_SECRET=<Google OAuth Client Secret>
```

> **Valores reais estao no Vercel Dashboard** (Environment Variables) e no `.env.local` local.
> Nao comitar secrets — GitHub Push Protection bloqueia automaticamente.

.env.local local tambem possui as mesmas variaveis para dev.

## Migrations

Unica migration aplicada: `drizzle/0000_abandoned_betty_ross.sql`

Contem todas as tabelas:
- `usf` — Unidades de Saude da Familia
- `notificacao` — Notificacoes de incidentes (com campos novos: classificacao_incidente, local_especifico, severidade)
- `usuario` — Usuarios do dashboard

Comando: `npx auth secret` gerou AUTH_SECRET.

## Testes

11 arquivos de teste, 81+ testes passando:
- `src/lib/middleware/__tests__/anonimizacao.test.ts` — 7 testes LGPD
- `src/lib/db/__tests__/usf.test.ts` — 4 testes USF
- `src/components/formulario/__tests__/formulario.test.tsx` — 10+ testes formulario
- `src/app/api/usf/__tests__/route.test.ts` — testes API USF
- `src/app/api/notificar/__tests__/route.test.ts` — testes API notificar
- `src/lib/services/__tests__/agregacao.test.ts` — testes agregacao
- `src/lib/services/__tests__/exportacao.test.ts` — testes exportacao
- `src/app/api/gestao/dashboard/graficos/__tests__/route.test.ts` — testes API graficos
- `src/app/api/gestao/exportar/dados/__tests__/route.test.ts` — testes API exportacao

## Qualidade

- `npm run lint` — sem erros (src/)
- `npm run typecheck` — limpo
- `npm run test` — 81+ passam, 3 suites .aiox-core falham (pre-existente, fora do escopo)
- QA Gate Epic 3: PASS

## Proximos Steps (prioridade)

### Curto prazo (proxima sessao)
1. **Login OAuth:** User story para criar uma USF e notificacao de teste via API para popular o banco (sem depender de email @pinhais.pr.gov.br)
2. **Seed data:** Criar script de seed para USFs de Pinhais e dados de demonstracao
3. **Testes E2E:** Playwright para fluxo completo
4. **Testes de carga:** k6 para 100+ acessos simultaneos

### Medio prazo
5. Job semanal de exportacao automatica (Cron Vercel)
6. Relatorios PDF
7. Integracao direta com NotiVISA

### Longo prazo
8. Workflow Comissao de Seguranca do Paciente
9. Dominio oficial pinhais.pr.gov.br
10. QR Code impresso nas USFs

## Comandos Uteis

```powershell
# Dev
npm run dev              # Servidor local
npm run build            # Build producao
npm run lint             # ESLint
npm run typecheck        # TypeScript check
npm run test             # Testes
npm run test:watch       # Testes watch mode

# DB
npx drizzle-kit push     # Aplicar schema ao banco
npx drizzle-kit generate # Gerar migracoes

# Deploy
npx vercel deploy --prod # Deploy producao
npx vercel env add       # Adicionar env var
npx vercel logs          # Logs do deploy

# Auth
npx auth secret          # Gerar AUTH_SECRET
```

## Arquivos Chave

| Arquivo | Descricao |
|---------|-----------|
| `vercel.json` | Config de deploy Vercel |
| `.env.local` | Varaveis de ambiente locais (NAO COMITAR) |
| `drizzle/0000_abandoned_betty_ross.sql` | Migracao do banco |
| `src/lib/db/schema.ts` | Schema Drizzle completo |
| `src/lib/middleware/anonimizacao.ts` | Middleware LGPD (persistencia + exportacao) |
| `src/lib/services/agregacao.ts` | Servico de agregacao para graficos |
| `src/lib/services/exportacao.ts` | Servico de exportacao CSV/JSON |
| `src/lib/auth/auth.ts` | NextAuth v5 com OAuth Google |
| `middleware.ts` | Protecao de rotas /gestao/* |
| `next.config.ts` | Config Next.js (assetPrefix, redirects) |
| `docs/prd.md` | PRD atualizado v1.1 |
| `docs/architecture.md` | Arquitetura atualizada v1.1 |
| `docs/qa/gates/3.0-dashboard-avancado-exportacao.yml` | QA Gate PASS |

## Notas Finais

- Todo o codigo implementado esta em main (commits: d47cbbe, 3e6edef)
- Nenhuma branch criada — fluxo YOLO (commit direto em main)
- 81+ testes passando antes do deploy
- Vercel CLI autenticado como `lincolnamerico`
- Proximo dev pode iniciar com `npm run dev` (desde que .env.local exista)
